let language = 'Ruby';
let lenguage = 'Go';

console.log(language) // error, because the variable 'language' has already been declareted

/////////////////////////////////////////////////////////

var language = 'Ruby';
var lenguage = 'Go';

console.log(language) // 'var' for to declare variables maybe causes seriously problems because 
// the variable 'language' will be over writing